---
- name: update app {{ app_name }}
  hosts: tag_Roles_nginx:tag_Roles_nginx,*:tag_Roles_*,nginx:tag_Roles_*,nginx,*
  remote_user: "{{ remote_user }}"
  roles:
  - { role: app_deploy }
  vars:
  - app_name: "symanex-site"
  - app_repo: "symanex/symanex-site"
  - app_version: "master"
  - app_host: "www.symanex.com"
